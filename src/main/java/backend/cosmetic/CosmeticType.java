package backend.cosmetic;

/**
 * Whether a cosmetic is an avatar (round profile picture) or a banner
 * (the wide profile header). Mirrors the frontend {@code CosmeticType}.
 */
public enum CosmeticType {
    AVATAR,
    BANNER
}
